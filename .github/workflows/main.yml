name: Mon premier workflow
on: push
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step 1 
        id: step1
        run: echo ::set-output name=fruit::pomme
      - name: step 2
        run: | 
            echo ::notice::Mon message de notice
            echo ::warning::Je suis un warning
            echo ::error::Je suis une erreur
       

    
