name: Mon premier workflow
on: push
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step 1 
        id: step1
        run: echo ::set-output name=fruit::pomme
      - name: step 2
        if: steps.step1.outputs.fruit != 'poire'
        run: echo ::error::Je suis une erreur output step1 is ${{ steps.step1.outputs.fruit }} should be poire 
       

    
