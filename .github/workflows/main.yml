name: Mon premier workflow
on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ 1 + 1 }}
      - run: echo ${{ 'main' != 'top' }}
      - run: echo ${{ github.ref == 'heads/refs/main' }}

    
