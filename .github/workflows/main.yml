name: Mon premier workflow
on: push
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - run: |
            pwd
          shell: sh 
        - run: |
            pwd
          shell: pwsh
