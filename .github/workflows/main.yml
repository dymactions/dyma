name: Mon premier workflow
on: push
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step 1 
        id: step1
        run: echo fruit=pomme >> $GITHUB_OUTPUT
      - name: step 2
        run: echo ${{ steps.step1.outputs.fruit }}
    
       

    
